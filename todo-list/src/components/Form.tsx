import React from "react";

const Form = (): React.ReactElement => {
  return (
    <form>
      <input type="text" />
      <button type="submit">Add Todo</button>
    </form>
  );
};

export default Form;
